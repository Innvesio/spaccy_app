import { View, Text } from "react-native";
import React from "react";

const conversationCard = ({ detals }) => {
  return (
    <View>
      <Text>conversationCard</Text>
    </View>
  );
};

export default conversationCard;
